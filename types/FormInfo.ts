export interface FormInfo {
  brand: string;
  rentalPrice: string;
  minMileage: string;
  maxMileage: string;
}

export interface BookFormInfo {
  email: string;
  name: string;
  date: string;
  comment: string;
}
